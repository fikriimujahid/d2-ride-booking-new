# ============================================================================
# BACKEND API ENVIRONMENT VARIABLES - TEMPLATE
# ============================================================================
# Copy this file to .env for local development
# Production values are injected via EC2 user-data or SSM Parameter Store

# ----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# ----------------------------------------------------------------------------
NODE_ENV=dev
PORT=3000

# ----------------------------------------------------------------------------
# AWS CONFIGURATION
# ----------------------------------------------------------------------------
AWS_REGION=ap-southeast-1

# ----------------------------------------------------------------------------
# COGNITO CONFIGURATION (JWT Verification)
# ----------------------------------------------------------------------------
# Format: <region>_<pool-id> (e.g., ap-southeast-1_abc123xyz)
COGNITO_USER_POOL_ID=ap-southeast-1_XXXXXXXXX

# Client ID from Cognito User Pool (for JWT audience validation)
COGNITO_CLIENT_ID=your-client-id-here

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION (MySQL)
# ----------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ridebooking
DB_USER=app_user

# ----------------------------------------------------------------------------
# OPTIONAL: DATABASE TLS SETTINGS
# ----------------------------------------------------------------------------
# By default, TLS is enabled and certificates are verified.
# If you see: "self-signed certificate in certificate chain"
# 1) Prefer: provide the correct CA bundle
#    DB_SSL_CA_PATH=/path/to/ca-bundle.pem
#    # or base64-encoded PEM:
#    # DB_SSL_CA_B64=...
# 2) Dev-only workaround (NOT recommended for prod):
#    DB_SSL_REJECT_UNAUTHORIZED=false
#
# Disable TLS entirely (not compatible with IAM auth; for local-only testing):
# DB_SSL=false

# ----------------------------------------------------------------------------
# OPTIONAL: DATABASE PASSWORD FOR LOCAL DEV
# ----------------------------------------------------------------------------
# NOTE: Production uses IAM DB Authentication (no password)
# Uncomment for local MySQL without IAM auth
# DB_PASSWORD=local_dev_password

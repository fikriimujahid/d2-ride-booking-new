# Web Admin (Vite) - DEV env example
# NOTE: Vite only exposes variables prefixed with VITE_ to the browser.

# Backend API base URL (do not hardcode in code)
VITE_API_BASE_URL=https://api.d2.fikri.dev

# Cognito (manual auth: username/password, no Hosted UI)
VITE_COGNITO_USER_POOL_ID=ap-southeast-1_XXXXXXXXX
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
